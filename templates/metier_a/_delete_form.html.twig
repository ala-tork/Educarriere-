<form method="post" action="{{ path('app_metier_a_delete', {'id': metier_a.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metier_a.id) }}">
    <button class="btn">Delete</button>
</form>
