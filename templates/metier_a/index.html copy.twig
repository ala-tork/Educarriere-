{% extends 'dashbord_ctrl/DashbordBase.html.twig' %}

{% block MainBody %}
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <div class="content-header">
            <div class="container-fluid">
                <div class="row mb-2">
                    <div class="col-sm-6">
                        <h1 class="m-0">Métier Avenir</h1>
                    </div><!-- /.col -->

                </div><!-- /.row -->
            </div><!-- /.container-fluid -->
        </div>
        <!-- /.content-header -->

        <!-- Main content -->
        <section class="content">
            <div class="container-fluid " style="padding-top: 40px">
                <!-- Small boxes (Stat box) -->

                <a href="{{ path('app_metier_a_new') }}" class="btn btn-success" >Create new</a>
                <table class="table mt-6" style="padding-top: 40px">
                    <thead>
                    <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Salaire</th>
                <th>Chomage</th>
            {#   <th>Description</th> #}
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for metier_a in metier_as %}
            <tr>
                <td>{{ metier_a.id }}</td>
                <td>{{ metier_a.nom }}</td>
                <td>{{ metier_a.salaire }}</td>
                <td>{{ metier_a.chomage }}</td>
           {#   <td>{{ metier_a.Description }}</td> #}

            {#  <td>    <a href="{{ path('app_description_index') }}"  ><i class="fa fa-book" aria-hidden="true"></i>
              </a> </td> #}

                <td>
          
        <a href="{{ path('app_metier_a_show', {'id': metier_a.id}) }}" ><i class="fa-regular fa-eye"></i></a>



        <a href="{{ path('app_metier_a_edit', {'id': metier_a.id})  }}" ><i class="fa-solid fa-pen-to-square"></i></a>
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        
        </tbody>
    </table>

    </div><!-- /.container-fluid -->
        </section>
        <!-- /.content -->
    </div>
    <!-- /.content-wrapper -->
{% endblock %}